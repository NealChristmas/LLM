# D03 自测参考答案

先完成[正文末尾的自测](README.md)，再核对。答案重点是概念之间的因果关系，不要求背诵小数。

1. **不能。** Logit 是未归一化分数，可以为负，也不要求总和为 1。Softmax 将整组 Logit 转成非负、总和为 1 的概率，同时保留相对大小关系。
2. **0.8 对应的交叉熵更小。** 因为 L=−ln(p目标)，正确 Token 的概率越大，损失越小。
3. **准确率不能区分，交叉熵可以。** 两个模型都把正确 Token 排第一，所以这一次准确率都为 1；模型 B 给正确 Token 更高概率，因此交叉熵更低。
4. 梯度为 **p−y=(−0.3,0.2,0.1)**。梯度下降减去梯度，所以第一个正确 Logit 上升，后两个错误 Logit 下降。
5. 最可能是**过拟合**。训练集继续改善而验证集恶化，说明泛化变差；选择检查点不能只看训练损失，应结合验证表现。
6. 人会根据验证结果反复选择模型、超参数和停止点，相当于逐渐针对验证集优化方案。测试集用于方案确定后的独立最终评估，减少这种选择偏差。
7. **不能。** 还需要与企业问答场景匹配的数据，以及事实正确性、引用可靠性、指令遵循、失败类型、延迟和成本等评估；下一 Token 损失只覆盖其中一部分。

第 4 题不清楚时，回看正文第 3 节的 ∂L/∂zⱼ=pⱼ−yⱼ；第 5、6 题不清楚时，回看第 4 节的数据职责划分。
