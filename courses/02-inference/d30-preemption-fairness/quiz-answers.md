# D30 自测题参考答案

1. FCFS 可能被早到长请求阻塞；永远优先短请求可能使长请求持续饥饿。
2. 保留或换出需要额外存储和传输；释放后重算节省当前显存，但恢复时增加 Prefill 计算和等待。
3. 持续到来的高优先级请求会让普通请求永远没有机会；老化或配额为它们保证最低进展。
4. 不能。长期输入大于输出时队列仍会增长，需要限流、拒绝、扩容或路由等准入措施。
5. 不能只因总吞吐提高就判定成功；若服务目标包含普通租户的尾部延迟，该优化就破坏了公平性和既定服务目标。
6. 用户若能通过虚报短长度获得优先级，实际长生成会占用更多资源并破坏策略公平性。
