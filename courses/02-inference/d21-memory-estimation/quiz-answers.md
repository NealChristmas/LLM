# D21 自测题参考答案

1. 10B×2 字节约为 20 GB，未计量化元数据或运行时空间。
2. 约 4 倍，因为 KV Cache 在其他条件不变时近似随序列长度线性增长。
3. 主体约减半。
4. 还存在量化尺度、元数据、对齐、未量化层、KV Cache、激活和运行时工作区。
5. KV Cache 按每个活动请求已经占用的序列位置增长；相同请求数可能对应完全不同的长度总和。
6. 没有给激活、Kernel 工作区、内存池和瞬时波动留余量，实际执行容易内存不足。
