# D22 自测题参考答案

1. TTFT 还包含上游处理和排队；必须拆出各阶段耗时，才能判断是否主要是 Prefill。
2. 不能。输入长度直接影响 Prefill 工作量，两次负载不等价，差异无法归因于优化。
3. 检查批次太小、请求供给不足、CPU 预处理、Kernel 启动、同步、跨卡通信和调度等待。
4. 示例：“Decode 批次太小导致并行度不足；保持其他条件不变提高稳定并发后，每卡输出吞吐应上升。”它给出了原因、改动与预期证据。
5. 系统可能等待或组合更大批次来提高总体工作效率，因此单位时间处理更多 Token，却让部分请求排队更久。
6. 至少缺少模型与量化格式、硬件和引擎、输入输出分布、并发或到达率、比较指标、统计范围以及质量变化。
